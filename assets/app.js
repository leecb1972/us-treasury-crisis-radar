function show(id,btn){document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));const target=document.getElementById(id);if(target)target.classList.add('active');document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));if(btn)btn.classList.add('active');}
async function loadDebt(){const els=document.querySelectorAll('[data-debt]');if(!els.length)return;els.forEach(e=>e.textContent='Loading…');try{const u='https://api.fiscaldata.treasury.gov/services/api/fiscal_service/v2/accounting/od/debt_to_penny?sort=-record_date&page[size]=1';const r=await fetch(u);if(!r.ok)throw new Error('HTTP '+r.status);const j=await r.json();const x=j.data[0];const total=Number(x.debt_held_by_the_public)+Number(x.intragovernmental_holdings);const value='$'+(total/1e12).toFixed(3)+'T · '+x.record_date;els.forEach(e=>e.textContent=value);}catch(e){els.forEach(e=>e.textContent='Unavailable — open Treasury Fiscal Data');}}
function init(){loadDebt();}
document.addEventListener('DOMContentLoaded',init);
